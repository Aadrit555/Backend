# Core
fastapi>=0.111,<1
uvicorn[standard]>=0.30,<1
pydantic-settings>=2,<3
sqlalchemy>=2,<3

# Groq orchestrator
groq>=0.9,<1

# ML backends (install as needed per adapter)
# autogluon.tabular
# ultralytics
# llama-factory  (pip install llamafactory)
# unsloth

# Data
pandas>=2,<3
pyyaml>=6,<7

# Testing
pytest>=8,<9
pytest-asyncio>=0.23,<1
httpx>=0.27,<1

# WebSocket support (included in uvicorn[standard])
websockets>=12,<14
